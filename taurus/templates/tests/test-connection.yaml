apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taurus.fullname" . }}-test-connection"
  labels:
    {{- include "taurus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taurus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
