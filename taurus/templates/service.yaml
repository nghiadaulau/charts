apiVersion: v1
kind: Service
metadata:
  name: {{ include "taurus.fullname" . }}
  labels:
    {{- include "taurus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taurus.selectorLabels" . | nindent 4 }}
